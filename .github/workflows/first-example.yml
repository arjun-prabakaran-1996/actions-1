name: first_workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest 
        steps:
            - name: checkout repo
              uses: actions/checkout@v4
              
            - name: welcome
              run: echo "welcome"

            - name: show files
              run: ls

            - name: print file
              run: cat README.md

        